PyQt6>=6.5
matplotlib>=3.6
numpy>=1.24

# Windows n'a pas de base de fuseaux horaires systeme : sans ce paquet de
# donnees, zoneinfo ne trouve pas "Europe/Paris" et le simulateur ne demarre pas.
tzdata; sys_platform == "win32"
